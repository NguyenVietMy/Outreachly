spring.application.name=Outreachly
server.port=8080

# Actuator configuration for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.health.defaults.enabled=true

# Database configuration from environment variables - temporarily disabled for testing
# spring.datasource.url=${JDBC_URL}
# spring.datasource.username=${DB_USER}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver

# Database connection settings - temporarily disabled for testing
# spring.datasource.hikari.connection-timeout=30000
# spring.datasource.hikari.maximum-pool-size=5
# spring.datasource.hikari.minimum-idle=1
# spring.datasource.hikari.connection-test-query=SELECT 1

# JPA/Hibernate configuration - temporarily disabled for testing
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.format_sql=true

# Make database connection optional during startup
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.defer-datasource-initialization=true
# spring.sql.init.mode=always

# Security configuration - allow health endpoints without authentication
spring.security.user.name=admin
spring.security.user.password=admin

